// enums.ts
export enum Side {
  Buy = "Buy",
  Sell = "Sell",
}

export enum ExecutionType {
  ImmediateOrCancel = "Immediate Or Cancel",
  Default = "Default",
  FillOrKill = "Fill or Kill",
  PostOnly = "Post-Only",
}
